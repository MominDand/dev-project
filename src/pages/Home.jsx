import Hero from '../components/Hero';
import CategoryGrid from '../components/CategoryGrid';

const Home = () => {
    return (
        <main>
            <Hero />
            <CategoryGrid />
        </main>
    );
};

export default Home;
